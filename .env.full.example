# 完整版（Demucs + basic-pitch）
COMPOSE_PROFILES=full
INSTALL_OPTIONAL=true
BACKEND_PRIVILEGED=true
BACKEND_SHM_SIZE=2gb

# Demucs 分离超时（秒）；NAS CPU 较慢时可再加大
SONG_TO_TAB_WORKER_TIMEOUT_SEPARATE=3600

# 宿主机对外端口（映射到容器内 Nginx 80）
HOST_PORT=60901
